# Feature idea comparison — 2026-09-07

Source: structured 2026-09-07 idea handoff. Later explicit decisions and current project rules take precedence. Status values describe the audited source, not aspiration. LAYER rows were rechecked on 2026-09-09; see [resource workspace integration](RESOURCE-WORKSPACE-2026-09-09.md). Other rows retain their original audit scope.

| ID | Requirement | Status | Evidence and reuse | Gap | Next step / acceptance criterion |
| --- | --- | --- | --- | --- | --- |
| LAYER-001 | Select a bounded map area and analyze its full vertical world volume. | Present for resource analysis; generic contract pending | Existing `atlasResources` bridge, `loadMapRegion`, `ores.scan`, `chunks.decode`. Button or Shift-drag uses inclusive world bounds; 18 material groups. | Generic shared `WorldRegion` remains INT-002; no arbitrary custom registry groups. | Preserve the narrow bridge during generic map coordination; see 2026-09-09 integration. |
| LAYER-002 | Browse a top-down view per height with slider, keys, wheel/touchpad and visible coordinates. | Present in bounded resource workspace | `OreWorkspaceView.layerMap`, `OreLayerWheel`, `OreSpatial.cell`: north-up Canvas, slider, keys, Option-scroll, exact block pin/coordinates. | The view is a specialized analytical detail, without Atlas rotation/zoom. Full spatial view is capped at 4,194,304 cells. | Keep pixel/coordinate and negative-boundary fixtures; extend zoom only if large-area readability requires it. |
| LAYER-003 | Show counts for the current layer and the entire selected volume, including proportions. | Present | `OreWorkspaceView.ledger`, shared material selection and `OreDistributionView`; synthetic clipping and conservation tests. | Configured categories are fixed; old measurements require recount for newly added materials. | Retain explicit denominators and missing-data gaps in future migrations. |
| LAYER-004 | Filter or skip to interesting/profitable layers. | Present as configurable count threshold | `OreWorkspaceView.step`, `OreLayerNavigation.next`: selected-material total and minimum count; arrows skip and wrap. Slider remains unrestricted. | This is a deposit-count threshold, not a mining-profit model; incomplete coverage is explicitly reported. | Preserve visible coverage and avoid interpreting absence in missing terrain. |
| LAYER-005 | Compare larger areas and show rich subareas and connected deposits. | Partially present | `OreArea.grouped`, geographic 16/64 tile comparison, count/rate ranking and tile handoff; `connectedCount` provides bounded four-neighbor same-material layer connectivity. | No three-dimensional cluster labeling, arbitrary subdivision editor or terrain-route export. | Add a versioned 3D adjacency definition and navigation adapter only after a separate integration design; keep unknown cells as connectivity boundaries. |
| MAP-001 | Reuse one area-selection mechanism across ownership, analysis and future actions. | Partially present | Ownership drag, two-point measurement and single-point portal planning all preserve world coordinates. | Each tool owns separate point/active/cancel state; cancellation is coordinated ad hoc in `app.js`. | Introduce a shared map-tool coordinator and world selection types before adding a layer drag tool. Tests must cover activation conflicts, Escape, dimension changes, rotation and mirroring. |
| MAP-002 | Combine information layers while keeping the map readable. | Present, extensible | Terrain/height, grid, signs, POIs, ownership, transport and portals can be combined; spoiler mode suppresses sensitive overlays. | No explicit collision/priority policy, preset system or readability rule exists for many simultaneous overlays. | Add layer metadata (priority, exclusivity, legend, privacy) and at least one saved analysis/transport preset without changing coordinate semantics. |
| MAP-003 | Preserve existing distance, travel-time and turn-by-turn navigation. | Present | `measure.js`, `navigation.js`, `transport.js` and `NavigationPack` implement distance/profile estimates, candidate routes, instructions and exports. | Movement speeds and some traversal assumptions remain unverified; live tracking is unavailable. | Keep visible assumptions and add verified RealmCraft movement profiles only from controlled evidence. |
| METRO-001 | Verify RealmCraft portal coordinate/linking mechanics before reliable planning. | Partially present | Detected portals, directed reported pairs and an explicitly assumed 8:1 converter exist. | Scale, search radius, Y influence, tie-breaking and reverse behavior are unverified. | Record controlled pairs with game version, orientation and both travel directions. Reliable prediction remains blocked until the evidence matrix is satisfied. |
| METRO-002 | Plan a four-direction, ring/cross-link portal and minecart network. | Partially present | `MetroStation`, `MetroLine`, `MetroEdge` and snapshot-local `MetroNetworkStore` model named directed connections without a second map/parser. | Stations are manual entries; no detector turns saved rails into a connection, and the metadata does not yet carry a source portal fingerprint. | Add a controlled portal-evidence workflow and decide whether a validated logical-world migration is safe before carrying plans to later snapshots. |
| METRO-003 | Display named, colored lines, transfer points and a schematic metro map. | Partially present | `MetroView` derives a deliberately non-geographic diagram from the same stored stations, lines and edges. | The atlas has no geographic metro overlay and transfers are derived from the selected route rather than separately annotated. | Add a shared map-tool coordinator, then connect the same graph to an opt-in geographic overlay without changing world-coordinate semantics. |
| METRO-004 | Include metro segments, transfers and walking legs in route instructions and travel estimates. | Partially present | `MetroNetworkStore.route` computes the lowest measured-duration directed route; `MetroView` emits a copyable station-by-station briefing. | Existing `NavigationPack` remains single-dimension terrain guidance; this first bridge does not merge live terrain walking with a cross-dimension metro route. | Define a dimension-aware navigation/export schema after portal mechanics and transfer timing have controlled evidence. |
| DEV-001 | Maintain a central feature map, architecture, data model and idea comparison. | Present after this audit | `docs/FEATURE-MAP.md`, `ARCHITECTURE.md`, `DATA-MODEL.md`, this file and the audit report. | The community-source packaging script does not yet include `docs/`. | Review documents for public content, then explicitly add `docs/` to packaging if desired. |
| DEV-002 | Make appropriate Codex prompts/skills available through developer options. | Partially present | macOS has full skill management/import/export and bundled development/platform/savegame/help skills. | The new codecheck skill is not bundled; Android and web have no skill management. | Decide which maintenance skills are safe/public and whether platform editions need export-only support before porting. |
| DEV-003 | Maintain a local, updateable index of changed files and project relationships. | Partially present | This audit generated `graphify-out/graph.json`, `graph.html` and `GRAPH_REPORT.md` for the 75-file Swift source corpus. | The graph is manual, Swift-source-only and includes heuristic edges; no automatic update hook or complete platform index exists. | Add an explicit documented update command and review gate before automation. A future run must identify changed files, refresh dependent symbols and retain source-backed confidence labels. |
| DEV-004 | Separate platform-neutral capabilities from platform-specific implementation for contributors/forks. | Partially present | Independent platform repositories and earlier parity reports exist; this audit adds a system architecture and shared-capability map. | Binary readers and feature definitions still drift across Python/Swift/Java/JS; no canonical cross-platform contract suite exists. | Create versioned synthetic fixtures plus expected neutral JSON for world/player/chest/chunk readers on every supported platform. |

## Recommended implementation order

1. Close the remaining audit gate defects: safe build default and Atlas test harness; retain the corrected Android version/backlog documentation.
2. Implement MAP-001 shared tool coordination and a versioned `WorldRegion` contract.
3. Build LAYER-001/003 on the existing Python decoder and aggregate results; then add LAYER-002 map linkage and LAYER-004/005.
4. Collect RealmCraft portal evidence for METRO-001 in an isolated demo/test world.
5. Define the metro graph and persistence scope, then implement METRO-002/003/004.
6. Establish cross-platform fixtures before porting layer or metro features.
